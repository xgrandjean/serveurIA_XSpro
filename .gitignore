# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.*.local

# Logs
*.log

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.idea/
.vscode/
*.sublime-workspace

# Build output (electron-builder)
dist/
release/


# Misc
*.pem

# Journal d'anomalies du canal MCP (phase beta) — donnees d'execution
logs/

# Memoire du dernier canal de remplissage choisi — etat local au Worker
.worker-canal.json

# Verrou du Worker — PID du processus en cours, etat local
.worker.lock

# Charges d'essai du mode standalone — elles portent la cle API recopiee depuis
# XSpro (bloc « ia » du payload), qui n'a rien a faire sur un depot public.
# Restent sur le disque de chacun ; sorties de l'index le 2026-09-20.
standalone/ia-config.json
standalone/standalone-payload*.json
